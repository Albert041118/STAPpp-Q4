This directory is reserved for generated/native build files if required by the course submission structure.

Current build method:
  cmake -S ../src -B ../build-codex
  cmake --build ../build-codex --config Debug

Build outputs are intentionally not committed.
